<p id="notice"><%= notice %></p>

<h1>Slacks</h1>

<table>
  <thead>
    <tr>
      <th>Webhook url</th>
      <th>Channel name</th>
      <th>Username</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @slacks.each do |slack| %>
      <tr>
        <td><%= slack.webhook_url %></td>
        <td><%= slack.channel_name %></td>
        <td><%= slack.username %></td>
        <td><%= link_to 'Show', slack %></td>
        <td><%= link_to 'Edit', edit_slack_path(slack) %></td>
        <td><%= link_to 'Destroy', slack, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Slack', new_slack_path %>
